check_code_change: pnpm

before_compile:
  - n 20.19.0
  - npm i -g pnpm

compile:
  - pnpm install --frozen-lockfile

before_deploy:
  - echo -n $SEED_STAGE_NAME > .sst/stage
  - if [ $SEED_SERVICE_PATH != "" ]; then cp .sst $SEED_SERVICE_FULLPATH/.sst; fi
